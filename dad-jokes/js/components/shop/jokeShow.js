/*import shopJoke from "../jokes-id";

export async function shopJoke() {
  const paramURL = new URLSearchParams(window.location.search);
  const joke = paramURL.get("joke");

  const textJoke = document.createElement("p");
  textJoke.textContent = joke;
  document.body.appendChild(textJoke);
}

export default shopJoke;*/
